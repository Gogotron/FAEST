\begin{frame}{QuickSilver proof system}
  $$\mathsf{sk}={k}\quad\mathsf{pk}=(x,y)\quad\mathsf{OWF}_k(x)=y$$
  \pause
  A $\mathsf{OWF}$ can be represented as a circuit of gates
  \begin{columns}[c] % align columns
    \begin{column}{.35\textwidth}
      \begin{center}
        \begin{overprint}
          \onslide<2-3>
            \begin{tikzpicture}[
              level 1/.style = {
                level distance   = 7mm,
              }, level 2/.style = {
                level distance   = 7mm,
                sibling distance = 20mm
              }, level 3/.style = {
                level distance   = 7mm,
                sibling distance = 10mm
              }, level 4/.style = {
                level distance   = 10mm,
                sibling distance = 5mm
              },
              grow = up,
              operation/.style={circle,draw=black,inner sep=1pt,outer sep=0pt},
              input-output/.style={circle,draw=none,inner sep=1pt,outer sep=0pt},
            ]
              \node[input-output] {$y$}
                child[<-] {
                  node[operation] {$+$}
                  child[<-] {
                    node[operation] {$\times$}
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_3$}
                      }
                      child[<-] {
                        node[input-output] {$k_3$}
                      }
                    }
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_2$}
                      }
                      child[<-] {
                        node[input-output] {$k_2$}
                      }
                    }
                  }
                  child[<-] { node[operation] {$\times$}
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_1$}
                      }
                      child[<-] {
                        node[input-output] {$k_1$}
                      }
                    }
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_0$}
                      }
                      child[<-] {
                        node[input-output] {$k_0$}
                      }
                    }
                  }
                }
              ;
            \end{tikzpicture}
          \onslide<4>
            \begin{tikzpicture}[
              level 1/.style = {
                level distance   = 7mm,
              }, level 2/.style = {
                level distance   = 7mm,
                sibling distance = 20mm
              }, level 3/.style = {
                level distance   = 7mm,
                sibling distance = 10mm
              }, level 4/.style = {
                level distance   = 10mm,
                sibling distance = 5mm
              },
              grow = up,
              operation/.style={circle,draw=black,inner sep=1pt,outer sep=0pt},
              input-output/.style={circle,draw=none,inner sep=1pt,outer sep=0pt},
              wire-commitment/.style={circle,draw=blue,fill=blue,inner sep=0.5pt,outer sep=0pt},
            ]
              \node[input-output] {$y$}
                child[<-] {
                  node[operation] {$+$}
                  child[<-] {
                    node[operation] {$\times$}
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_3$}
                        edge from parent node[wire-commitment] {}
                      }
                      child[<-] {
                        node[input-output] {$k_3$}
                        edge from parent node[wire-commitment] {}
                      }
                      edge from parent node[wire-commitment] {}
                    }
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_2$}
                        edge from parent node[wire-commitment] {}
                      }
                      child[<-] {
                        node[input-output] {$k_2$}
                        edge from parent node[wire-commitment] {}
                      }
                      edge from parent node[wire-commitment] {}
                    }
                    edge from parent node[wire-commitment] {}
                  }
                  child[<-] {
                    node[operation] {$\times$}
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_1$}
                        edge from parent node[wire-commitment] {}
                      }
                      child[<-] {
                        node[input-output] {$k_1$}
                        edge from parent node[wire-commitment] {}
                      }
                      edge from parent node[wire-commitment] {}
                    }
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_0$}
                        edge from parent node[wire-commitment] {}
                      }
                      child[<-] {
                        node[input-output] {$k_0$}
                        edge from parent node[wire-commitment] {}
                      }
                      edge from parent node[wire-commitment] {}
                    }
                    edge from parent node[wire-commitment] {}
                  }
                  edge from parent node[wire-commitment] {}
                }
              ;
            \end{tikzpicture}
          \onslide<5->
            \begin{tikzpicture}[
              level 1/.style = {
                level distance   = 7mm,
              }, level 2/.style = {
                level distance   = 7mm,
                sibling distance = 20mm
              }, level 3/.style = {
                level distance   = 7mm,
                sibling distance = 10mm
              }, level 4/.style = {
                level distance   = 10mm,
                sibling distance = 5mm
              },
              grow = up,
              operation/.style={circle,draw=black,inner sep=1pt,outer sep=0pt},
              input-output/.style={circle,draw=none,inner sep=1pt,outer sep=0pt},
              wire-commitment/.style={circle,draw=blue,fill=blue,inner sep=0.5pt,outer sep=0pt},
            ]
              \node[input-output] {$y$}
                child[<-] {
                  node[operation] {$+$}
                  child[<-] {
                    node[operation] {$\times$}
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_3$}
                        edge from parent node[wire-commitment] {}
                      }
                      child[<-] {
                        node[input-output] {$k_3$}
                        edge from parent node[wire-commitment] {}
                      }
                    }
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_2$}
                        edge from parent node[wire-commitment] {}
                      }
                      child[<-] {
                        node[input-output] {$k_2$}
                        edge from parent node[wire-commitment] {}
                      }
                    }
                    edge from parent node[wire-commitment] {}
                  }
                  child[<-] {
                    node[operation] {$\times$}
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_1$}
                        edge from parent node[wire-commitment] {}
                      }
                      child[<-] {
                        node[input-output] {$k_1$}
                        edge from parent node[wire-commitment] {}
                      }
                    }
                    child[<-] {
                      node[operation] {$+$}
                      child[<-] {
                        node[input-output] {$x_0$}
                        edge from parent node[wire-commitment] {}
                      }
                      child[<-] {
                        node[input-output] {$k_0$}
                        edge from parent node[wire-commitment] {}
                      }
                    }
                    edge from parent node[wire-commitment] {}
                  }
                }
              ;
            \end{tikzpicture}
        \end{overprint}
      \end{center}
    \end{column}%
    \hfill%
    \begin{column}{.65\textwidth}
      \pause
      To prove knowledge of $x$:
      \begin{itemize}
        \pause
        \item Commit to wire values (use VOLE)
        \pause
        \item Prove additive gates (free)
        \pause
        \item Prove multiplicative gates (complicated) \\
          Multiplication raises degree
      \end{itemize}
    \end{column}%
  \end{columns}
\end{frame}


\begin{frame}{How do we prove products?}
  How do we prove that $a\cdot b=c$ from $\left<a\right>^1,\left<b\right>^1,\left<c\right>^1$?
  \pause
  \begin{itemize}
    \item Compute $\left<a{\cdot}b-c\right>^2=\left<a\right>^1{\cdot}\left<b\right>^1-\left<c\right>^2$, \\
      should be commitment to 0.
    \pause
    \item Prover reveals masked coefficients of the polynomial.
    \pause
    \item Verifier checks evaluation at $\Delta$ and leading coefficient.
  \end{itemize}
\end{frame}
